{
  "name": "anyeditable-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @p/anyeditable",
    "test": "npm test -w @p/anyeditable",
    "typecheck": "npm run typecheck -w @p/anyeditable",
    "verify": "npm run typecheck && npm test && npm run build",
    "dev": "npm run dev -w composer-demo",
    "demo:build": "npm run build -w composer-demo"
  }
}
